{
  "name": "@sih-rrt/prisma-sih",
  "version": "1.0.0",
  "description": "A minimal prisma client for sih-rtt",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "db:generate": "dotenv -- prisma generate",
    "postinstall": "npx tsc && cp src/index.d.ts dist/index.d.ts && npx prisma@5.6.0 generate"
  },
  "author": "Anmol Sudhir <anmolsudhir.2001@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "prisma": "^5.6.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0"
  }
}
